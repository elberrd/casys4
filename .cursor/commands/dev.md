# Development Orchestrator

Execute the task below. Spawn subagents as needed — you are the orchestrator.

## Task

$ARGUMENTS

## Guidelines

- Use `task-sequencer` for planning if the task is complex
- Use `senior-orchestrator` to track completion against `ai_docs/todo.md`
- Use `ui-component-researcher` for UI component questions
- Follow all project rules in `AGENTS.md`
